{
  "name": "graph-ui-ts",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "srv": "tsc && vite build && vite preview --port 9000"
  },
  "devDependencies": {
    "typescript": "^4.6.4",
    "vite": "^3.2.0"
  },
  "dependencies": {
    "@memgraph/orb": "^0.1.4"
  }
}
